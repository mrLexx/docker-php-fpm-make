on:
  workflow_call:
    inputs:
      version:
        required: true
        type: string

jobs:
  print-username:
    runs-on: ubuntu-latest

    steps:
      - name: Print the input name to STDOUT
        run: echo The version is ${{ inputs.version }}
